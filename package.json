{
  "scripts": {
    "build": "spago build",
    "test": "spago -x spago.test.dhall test -m Test.Main",
    "check": "purs-tidy check src test"
  },
  "dependencies": {
    "binaryen": "^112.0.0"
  },
  "devDependencies": {
    "@babel/parser": "^7.21.4"
  }
}
